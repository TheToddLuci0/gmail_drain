[project]
name = "gmail-drain"
version = "0.1.0"
description = "Empty gmail inboxes with speed"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "google-api-python-client>=2.193.0",
    "google-auth-httplib2>=0.3.1",
    "google-auth-oauthlib>=1.3.1",
]
